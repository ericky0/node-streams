{
  "name": "nodestreams",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "tsnd --respawn --transpile-only src/client.ts",
    "server": "tsnd --respawn --transpile-only src/server.ts",
    "dev": "tsnd --respawn --transpile-only index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@types/axios": "^0.14.0",
    "axios": "^1.6.3"
  }
}
